// Needed for page login
exports.LOGIN_USERNAME = "";
exports.LOGIN_PASSWORD = "";

// The ID and token of the webhook you wish to send the data to
exports.WEBHOOK_ID = "";
exports.WEBHOOK_TOKEN = "";

// the JSON_URL is for fetch functions which write and get the latest news title, in order to verify if a newer post exists.
// Just use any online JSON host that allows GET and POST methods.
// Probably could circumvent fetching alltogether by using a local JSON file and fs if you wish to run the script on your PC, but on cloud
// hosting such as Heroku you will have to use third-party storage services.
exports.BACKGROUND_IMAGE = "";
exports.JSON_URL = "";